
import { AppItem, TodayStory } from './types';

export const MOCK_APPS_ZH: AppItem[] = [];
export const TODAY_STORIES_ZH: TodayStory[] = [];

export const MOCK_APPS = MOCK_APPS_ZH;
export const TODAY_STORIES = TODAY_STORIES_ZH;
